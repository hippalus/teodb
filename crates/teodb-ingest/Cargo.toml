[package]
name = "teodb-ingest"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Durable WAL-backed ingestion, buffering, replay, and flush for TeoDB"
license.workspace = true

[dependencies]
teodb-core = { path = "../teodb-core" }
teodb-catalog = { path = "../teodb-catalog" }
teodb-storage = { path = "../teodb-storage" }

# Arrow
arrow = { workspace = true }

# Async
backon = { workspace = true }
tokio = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Observability
tracing = { workspace = true }

# Concurrency
parking_lot = { workspace = true }
moka = { workspace = true }
papaya = { workspace = true }

# Identifiers
uuid = { workspace = true }

# Time
chrono = { workspace = true }

[dev-dependencies]
teodb-test-support = { path = "../teodb-test-support" }
async-trait = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
